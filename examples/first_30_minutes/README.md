# First 30 Minutes Example

This small project backs `docs/FIRST_30_MINUTES.md`. It exercises the normal
new-user loop: run source, run through the VM, statically check, build a native
binary, and execute the binary.
